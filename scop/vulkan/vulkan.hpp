#include <iostream>
#include <vulkan/vulkan.hpp>

class Scop_vulkan {
    public:
        Scop_vulkan();
        Scop_vulkan(int a);
        Scop_vulkan& operator=(const Scop_vulkan &copy);
        ~Scop_vulkan();
    protected:
    private:
};